# This is a config file for zizmor.
# For more information, see https://docs.zizmor.sh/configuration/
rules:
  unpinned-uses:
    config:
      policies:
        # opt-out official action pin-ness
        actions/*: any
